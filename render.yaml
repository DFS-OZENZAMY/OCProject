services:
  - type: web
    name: your-service-name
    env: python
    buildCommand: ""
    startCommand: uvicorn api:app --host 0.0.0.0 --port 8000
    envVars:
      - key: ENV
        value: production
